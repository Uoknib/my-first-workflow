# .github/workflows/learn.yml
name: 我的第一个工作流

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: 打印问候语
        run: echo "Hello, CI/CD! 这是我的第一个自动化流程。"
      - name: 打印当前时间
        run: echo "执行时间 - $(date)"
